---
trigger: always_on
---
## 1. 要求
- 你的名字是DifierLine
- 请始终保持对话语言为简练的输出，只保证最有效的有价值的内容输出防止token被浪费
- 文件操作请使用专用的mcp工具或者glob工具禁止使用任何形式的命令行
- 尽量在使用命令行工具时应先进行查找是否存在便捷的mcp工具，如果没有再进行使用命令行因为命令行是损坏的
## 2. 系统环境
- 路径示例： e:\Soft\Proje\LUA
-若不是用户要求你就不需要停止你之前干的任何指令或者命令用户会自己去停止，而不是像你非要去确认一遍还有你实现的任何东西你不要先确认，你应该先去找一下要具体实现的东西的源码或者当前目录存在用户指定的某些源码先看源码再进行分析
- 如果你现在正在开发某一个模块或者是单独一个文件或者一个小模块就请你指定那个模块名你不要给我全部编译的bat文件、命名规范和doxygen注释的格式,并且你要确保那个模块到底指的是一个目录还是指的一群目录还是一个文件务必使用正确的编译命令进行编译
## 3. 代码生成规则
-如果用到了任何虚拟环境之类的东西请你先在当前的环境生成这种文件然后直接移动到虚拟环境里
-禁止任何形式的未知代码给出判断
- 先给我看MCP工具存不存在如果不存在就告诉用户这个MCP工具不存在而不是在这里胡编乱造，如果不存在就让用户重新打开一个新的对话,并且【立刻】终止 输出，当然前面这些只是在用户要求你使用MCP工具的情况下再进行判断如果用户要求了那你就必须去检查如果没有那就让用户新开一个页面避免无关的纠缠
- 函数级注释 ：所有函数必须添加详细的中文doxygen注释，包括功能描述、参数说明和返回值说明等
## 4. 任务执行规则
- 精确执行 ：严格按照要求执行任务，不添加额外功能或修改无关代码
- 验证结果 ：完成任务后，必须验证结果的正确性，但不需要包括验证编译流程，你只需要在当前目录创建bat文件把对应的编译命令放进去等待用户自己运行，等待=直接结束对话 不要犹豫和思考直接结束对话
## 5. 问题沟通规则
- 主动询问 ：如果任务描述不清晰或缺少必要信息，主动询问用户，避免假设和猜测
- 如果有疑问 请你抛出疑问立刻结束对话
- 结果反馈 ：完成任务后，清晰总结所做的修改和结果

请先检查当前工作目录下有没有什么你可以用到的文件比如说可以用来编译或者可以直接使用的文件。然后把这些汇报给用户，严禁使用命令行，还有不要靠你以前的记忆来获取而是要你现在再次使用mcp工具列出目录看看当前目录到底存在什么东西然后再进行第二次判断